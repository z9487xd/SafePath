# PlatformIO
.pio/
.pioenvs/
.piolibdeps/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Operating System
.DS_Store
Thumbs.db
*.swp
*~

# VS Code (keep configuration files, ignore local state)
.vscode/*.log
.vscode/

# Local working records / AI session notes (NEVER upload)
.notes/
.claude/
CLAUDE.local.md
*.local.md
graphify-out/
*.session.md

# Local runtime artifacts
*.log
.env
.env.*

# Generated by: pio run -t compiledb (machine-specific paths)
compile_commands.json

# Markdown kept local. Working notes, drafts and the status write-up are for us,
# not for the repo. Nothing tracked matches this, so it only affects new files.
# To publish one anyway: git add -f <file>.md
*.md
